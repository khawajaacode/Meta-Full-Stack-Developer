# React Calculator App

A simple calculator built with **React** that performs the four basic arithmetic operations:
**Addition, Subtraction, Multiplication, and Division**.

It also includes support for percentage, sign toggle, clear function, and keyboard input.

---

## 🚀 Features

* Perform basic math operations: ➕ Addition, ➖ Subtraction, ✖️ Multiplication, ➗ Division
* Percentage conversion (`%`)
* Toggle sign (`±`)
* Clear all (`AC`)
* Decimal point support (`.`)
* Keyboard support (`0-9`, `.`, `+ - * /`, Enter, Backspace, Escape)
* Responsive design for different screen sizes

---

## 📂 Project Structure

```
react-calculator-app/
├── public/
│   └── index.html
├── src/
│   ├── App.js
│   ├── index.js
│   └── index.css
├── package.json
└── README.md
```

---

## ⚙️ Installation & Setup

### Prerequisites

* Install [Node.js](https://nodejs.org/) (which includes npm)

### Steps

1. Clone or download the project files
2. Navigate into the project directory
3. Run the following command to install dependencies:

   ```bash
   npm install
   ```
4. Start the development server:

   ```bash
   npm start
   ```
5. Open the app in your browser at [http://localhost:3000](http://localhost:3000)

---

## 🧪 Usage

1. Click the calculator buttons to enter numbers and operators.
2. Press `=` to compute the result.
3. Use `AC` to clear the display.
4. Use `±` to toggle the sign of the number.
5. Use `%` to convert the number to a percentage.
6. You can also type calculations using your keyboard.

---

## 💡 Future Improvements

* Add calculation history display
* Support operator precedence (e.g., `2 + 3 * 4` handled as `2 + (3 * 4)`)
* Improve floating-point precision handling
* Add light/dark theme toggle

---

## 📜 License

This project was created for **educational purposes** as part of a React learning course.
